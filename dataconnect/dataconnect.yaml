specVersion: "v1beta"
serviceId: "oxela-service"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "oxela-database"
      cloudSql:
        instanceId: "oxela-cloudsqlinstance"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
